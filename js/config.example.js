/**
 * EmailJS Configuration Template
 * 
 * INSTRUCTIONS:
 * 1. Copy this file and rename the copy to 'config.js' in the same folder.
 * 2. Replace the placeholder values below with your actual EmailJS credentials.
 * 
 * Note: EmailJS public keys are intended to be visible in client-side code by design.
 * However, keeping 'config.js' in '.gitignore' ensures your active service details
 * remain clean out of your public GitHub repository listings and commit histories!
 */

export const EMAILJS_CONFIG = {
  serviceId: "YOUR_EMAILJS_SERVICE_ID",
  templateId: "YOUR_EMAILJS_TEMPLATE_ID",
  publicKey: "YOUR_EMAILJS_PUBLIC_KEY"
};
